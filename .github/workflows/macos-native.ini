[properties]
# macOS clang + gfortran configuration for Meson
# Use system clang for C/C++, conda gfortran for Fortran

[binaries]
c = 'clang'
cpp = 'clang++'
fortran = 'gfortran'
c_link_args = ['-fuse-ld=lld']
